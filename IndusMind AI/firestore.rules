rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permit read and write access for development and authenticated users
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
